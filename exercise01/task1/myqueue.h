#ifndef MYQUEUE_H_
#define MYQUEUE_H_

#include <stdbool.h>
#include <stdlib.h>

static void myqueue_init(myqueue* q) {}

static bool myqueue_is_empty(myqueue* q) {}

static void myqueue_enqueue(myqueue* q, int value) {}

static int myqueue_dequeue(myqueue* q) {}

static void myqueue_destroy(myqueue* q) {}

#endif
